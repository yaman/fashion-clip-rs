---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ${ENV_NAME}-embed
  namespace: argocd
spec:
  destination:
    namespace: embed
    server: ${CLUSTER_URL}
  project: default
  source:
    repoURL: https://gitlab.com/modanisatech/sherlock/personalization/deployment-artifacts.git
    path: embed/${ENV_NAME}
    targetRevision: master
